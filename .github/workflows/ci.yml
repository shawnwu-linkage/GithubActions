name: CI java test

on: workflow_dispatch

jobs:

  java-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print path
        run: pwd

      - name: clone repository
        run: git clone https://github.com/shawnwu-linkage/GithubActions.git

      - name: Print package
        run: ls